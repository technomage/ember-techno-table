<select disabled={{disabled}} {{action 'change' on='change'}}>
  {{#if prompt}}
    <option disabled>{{prompt}}</option>
  {{/if}}
  {{#each options as |opt|}}
    <option value={{opt}}>{{opt}}</option>
  {{/each}}
</select>
