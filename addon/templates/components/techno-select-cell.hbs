{{#if prompt}}
  {{#select-picker value=value content=options
    optionValuePath=valuePath
    optionLabelPath=labelPath
    optionGroupPath=groupPath
    nativeMobile=nativeMobile
    liveSearch=search
    multiple=multiple
    prompt=prompt}}{{prompt}}{{/select-picker}}
{{else}}
  {{select-picker value=value content=options
    optionValuePath=valuePath
    optionLabelPath=labelPath
    optionGroupPath=groupPath
    nativeMobile=nativeMobile
    liveSearch=search
    multiple=multiple
    prompt=prompt}}
{{/if}}
